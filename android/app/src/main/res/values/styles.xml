<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- 原始代码 -->
        <!-- <item name="android:editTextBackground">@drawable/rn_edit_text_material</item> -->
        <item
            name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item
            name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">
            @null</item><!--目前这个属性未知-->

        <item name="android:windowIsTranslucent">false
        </item><!--沉浸透明状态栏
        可选，窗口是否是半透明，设置状态栏的效果，这个值也会影响状态效果-->
        <item name="android:windowDisablePreview">true
        </item><!--冷启动是用到的属性，窗口不可见时的预览图，为true时点击app图标会慢一会-->
        <item name="android:windowBackground">@drawable/launch_screen</item><!--替换启动白屏的图片-->
        <!--<item
        name="android:windowBackground">@mipmap/ic_splash_logo</item>--><!--替换启动白屏的图片-->

        <!-- 在5.0后，增加了一个windowDrawsSystemBarBackgrounds属性，用来标志此窗口是否负责绘制系统栏背景，
        我们把它设成false，这样当它绘制windowBackground的时候，就会在NavigationBar之上。-->
        <item
            name="android:windowDrawsSystemBarBackgrounds">false</item>
        <item
            name="android:windowTranslucentStatus">true</item>

    </style>
</resources>